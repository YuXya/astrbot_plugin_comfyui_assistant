# 0.1.2 尺寸写入缩放与开始提示验证

77 项 Python 测试和 Ruff 检查通过。验证 1280×720 按 0.5 写入 640×360、0.25 和 0.1 等倍率、横竖尺寸、自动四舍五入到 8 像素倍数（中点向上）、保留模板默认值，以及倍率和资源范围校验。

开始提示支持尺寸和工作流占位符，显示原始请求尺寸。验证了模板校验、无尺寸时的默认提示，以及倍率和提示修改后旧任务的重启恢复、查询、重发保持原请求；正常成功任务仍仅发送开始提示和最终结果。

1500px 桌面和 390px 手机浏览器检查通过：倍率编辑、保存后重新打开、入口调整时保留倍率、开始提示字符串保存、原有前缀和上传等功能，无横向溢出。本次不生成真实图片、不发送 QQ，也不查看历史图片。

2026-09-10 已备份并更新当前 AstrBot 4.28.0 安装副本，重载后确认插件和实际管理页显示 0.1.2。工作流与服务设置表的安装前后校验一致；现有工作流默认倍率为 1，开始提示沿用默认文案。本次未提交或推送 GitHub。

# 0.1.1 默认提示词前缀验证

47 项 Python 测试与 Ruff 检查通过，新增覆盖：默认前缀为空、中文与英文及多行内容、按节点和输入字段绑定应用位置、入口顺序改变与失效、合并文本长度、模板和图片顺序保持。旧配置不需要数据库迁移。

使用替身分别恢复待提交及已提交任务：保存新前缀后，旧任务仍使用创建时的请求；反复查询和原图重发不会再次拼接或重新生成；后续新任务使用新前缀。正常成功任务仍只有开始提示和最终结果两次发送。

管理页在 1500px 桌面和 390px 手机宽度通过浏览器测试及页面截图检查，覆盖前缀保存、清空、重新打开、修改绑定后保留输入、删除选中入口必须重新选择，以及原有上传、删除、设置、重发功能；无横向溢出。

2026-09-10 已在 AstrBot 4.28.0 重载并确认 0.1.1 启用，实际管理页显示默认前缀和应用位置。安装前完成源码及数据库备份，安装后比对工作流与设置表，四个现有工作流及设置保持原值，前缀均为空。已有提交未确认记录保留，未重新生成或补发。

本次未触发真实生图或 QQ 发送，不打开历史图片。下方本机生成结果属于 0.1.0 验证记录。

# 0.1.0 测试状态

验收日期：2026-09-09。测试环境为 Windows、Python 3.12、AstrBot 4.28.0 和本机 ComfyUI。

## 自动化

34 项 Python 测试与 Ruff 检查通过，覆盖：

- LLM 结束后继续后台生成与发送，正常成功任务只有开始提示与最终图片两次发送。
- 宿主返回 False、明确拒绝、异常、超时；生成和发送分别记录，重发不增加生成次数。
- 重复查询、重复提交去重、重启恢复、提交未确认、发送中重启。
- 当前会话与发起人隔离，包括任务媒体引用和直接文件路径。
- 尺寸成对校验、尺寸范围、参数保持、图片数组占位符与 URL 混用顺序。
- 当前消息、引用消息、QQ 工具在当前 Agent 上下文中注入的图片。
- 宿主工具类型、回复抑制、插件加载与卸载。

管理页在 1500px 桌面和 390px 手机宽度完成浏览器测试，使用接口替身验证上传、删除、编辑、设置保存、重发及页面无横向溢出。页面不展示任务图片预览。

## 本机验证

使用新建几何图形和普通静物描述，对文生图、单图编辑、多图编辑分别测试默认 1024×1024、横图 1024×768、竖图 768×1024，共 9 次执行通过。

仅检查任务执行、输出文件和实际尺寸，没有目视检查画面效果。提交图与模板逐项比对，除指定输入、尺寸和随机种子外，模型、LoRA、扩写和采样参数保持不变。

真实 AstrBot 4.28.0 已验证插件安装、重载、四个工具注册、Plugin Pages 打开、设置与工作流说明保存。浏览器自动控制未完成真实 Plugin Pages 的文件选择，因此上传处理仅计入替身测试。

## 尚未验收

- 真实 QQ 收图、两条消息表现和用户发起的重发，仍需要在自己的测试会话验证。
- 流式回复和同轮混合其他业务工具的实际 QQ 行为。
- AstrBot 4.27.5 完整实机安装；兼容声明为 `>=4.27.5,<5`，测试结果不能等同于所有版本都实测通过。

`sent` 仅表示平台发送调用返回成功，不代表用户已经看到图片。首版作为测试版本发布。
